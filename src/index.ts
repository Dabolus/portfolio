// The order of the imports here might be important, so we need to disable this rule
/* tslint:disable:ordered-imports */

import './scripts/main';

import './styles/main';
import './styles/about-me';
