// TODO: dynamic import these files when esbuild makes it easier to do it
import * as enModule from '../locales/en.po';
import * as itModule from '../locales/it.po';

export const en = enModule;
export const it = itModule;
